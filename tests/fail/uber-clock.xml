<component xmlns="http://bender.igel.co.jp"
  xmlns:f="http://bender.igel.co.jp/f">
  <!-- FIXME inheritance is not yet implemented -->
  <title>Über Clock</title>

  <script>
    var interval;
    prototype.start = function()
    {
      if (interval) clearInterval(interval);
      interval =
        setInterval((function() { bender.notify(this, "@tick"); }).bind(this),
          this["rate-ms"]);
    };
  </script>

  <watch>
    <get event="@rendered">
      this.start();
    </get>
  </watch>

</component>
