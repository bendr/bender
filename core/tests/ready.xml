<app xmlns="http://bender.igel.co.jp" xmlns:html="http://www.w3.org/1999/xhtml">
  <title>@ready and @rendered events</title>
  <component id="ready-component">
    <stylesheet>
      .ready { background-color: #0f8; }
    </stylesheet>
    <controller>
      <listen event="@rendered" once="true">
        alert("(1) ready-component: {0} has sent a {1} event"
          .fmt(event.source.node.localName, event.type));
      </listen>
    </controller>
    <view>
      <html:p class="ready">
        <content/>
      </html:p>
    </view>
  </component>
  <controller>
    <listen event="@ready" once="true">
      alert("(2) App: {0} has sent a {1} event".fmt(event.source.node.localName,
        event.type));
    </listen>
  </controller>
  <view>
    <html:p>Alert when the application is ready.</html:p>
    <component ref="ready-component">(But first, when this component is
      ready.)</component>
  </view>
</app>
